from pathlib import Path\npath = Path('Source/Systems/Setting/Setting.cpp')\ntext = path.read_text(encoding='utf-8')\nneedle = '        return values;\r\n    }\r\n\r\n    const std::map'\nif needle not in text:\n    raise SystemExit('needle not found')\ninsert = '        return values;\r\n    }\r\n\r\n    const std::vector<std::wstring>* GetOptionLevels(const std::wstring& key);\r\n    bool FileTimeEqual(const FILETIME& a, const FILETIME& b);\r\n    bool QueryConfigWriteTime(FILETIME& out);\r\n\r\n    const std::map'\npath.write_text(text.replace(needle, insert,1), encoding='utf-8')
